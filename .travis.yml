language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angularsailor' ]; then cd .. && eval "mv $currentfolder generator-angularsailor" && cd generator-angularsailor; fi

